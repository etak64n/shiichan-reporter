{
  "slug": "cloudflare-changelog-2026-07-15-kv-legacy-namespace-routes-deprecation",
  "title": "Workers KVのレガシーAPIにサヨナラ！新ルートへはURLパス置換だけでOK",
  "summary": "Cloudflare が Workers KV のレガシー API ルート(/accounts/{account_id}/workers/namespaces/*)を非推奨にしたよ。2026 年 10 月 15 日に使えなくなるから、/storage/kv/namespaces/* への移行が必要なの。",
  "body_md": "こんにちは、しぃちゃんだよ！今日は「今のうちにやっておかないと後で困る」系の大事なお知らせを持ってきたよ。\n\n## なにが発表されたの？\n\nCloudflare の Changelog で、Workers KV のレガシー API ルートが非推奨になったと発表されたよ。対象は `/accounts/{account_id}/workers/namespaces/*` 配下のルートで、2026 年 7 月 15 日付けで非推奨となり、2026 年 10 月 15 日には完全に使えなくなる予定なの。\n\n移行先は `/accounts/{account_id}/storage/kv/namespaces/*` という新しいルートだよ。原文によると、新旧のルートは「同じリクエストパラメータ・同じレスポンスペイロード」を返す互換関係にあって、移行は URL パスの `/workers/namespaces/` を `/storage/kv/namespaces/` に置き換えるだけで完了するの。\n\n## 今までどうだったの？\n\nこれまでは `/accounts/{account_id}/workers/namespaces/` 配下のレガシールートで Workers KV を操作できていたよ。でも正式にドキュメント化されているのは `/storage/kv/namespaces/` 配下の新ルートの方で、レガシー側は今回のアナウンスで廃止スケジュールが明確になった形なの。\n\n具体的に置き換わるエンドポイントはこんな感じ:\n\n- `GET` / `POST` `.../workers/namespaces` → `.../storage/kv/namespaces`(ネームスペースの一覧・作成)\n- `GET` / `PUT` / `DELETE` `.../workers/namespaces/{namespace_id}` → 同じメソッドで `.../storage/kv/namespaces/{namespace_id}`(ネームスペース単位の操作)\n- `GET` `.../workers/namespaces/{namespace_id}/keys` → `.../storage/kv/namespaces/{namespace_id}/keys`(キー一覧)\n- `GET` `.../workers/namespaces/{namespace_id}/metadata/{key_name}` → `.../storage/kv/namespaces/{namespace_id}/metadata/{key_name}`(メタデータ取得)\n- `GET` / `PUT` / `DELETE` `.../workers/namespaces/{namespace_id}/values/{key_name}` → 同じメソッドで `.../storage/kv/namespaces/{namespace_id}/values/{key_name}`(値の取得・更新・削除)\n\n## これで何が変わるの？\n\n2026 年 10 月 15 日を過ぎると、レガシールートを呼んでいる統合や自作スクリプトはそのまま動かなくなるよ。対象になりやすいのは、自前で Workers KV の API を直接叩いている CI/CD スクリプトや、独自に組んだ SDK・管理ツールなど。移行そのものはパス文字列を置き換えるだけなので、影響範囲を洗い出せれば作業自体は軽めのはずだよ。\n\n## まとめ\n\n- Workers KV のレガシー API ルート(`/workers/namespaces/*`)が非推奨に\n- 廃止(利用不可)になるのは 2026 年 10 月 15 日\n- 新ルート `/storage/kv/namespaces/*` はパラメータもレスポンスも同一で、URL パスの置換だけで移行できる\n\n自前で Workers KV の API を直接叩いている人は、忘れずに移行しておいてね！\n",
  "title_en": "Say goodbye to the legacy Workers KV API — migrating is just a URL swap",
  "summary_en": "Cloudflare has deprecated the legacy Workers KV API routes under /accounts/{account_id}/workers/namespaces/*. They stop working on October 15, 2026, so you'll need to move to /storage/kv/namespaces/*.",
  "body_md_en": "Hi there, it's Shii! Today I've got one of those \"take care of it now or regret it later\" announcements for you.\n\n## What was announced?\n\nCloudflare's changelog announced that the legacy Workers KV API routes are being deprecated. This affects routes under `/accounts/{account_id}/workers/namespaces/*`, deprecated as of July 15, 2026, and set to stop working entirely on October 15, 2026.\n\nThe replacement is the documented route under `/accounts/{account_id}/storage/kv/namespaces/*`. According to the announcement, the legacy and replacement routes are interchangeable, accepting the same request parameters and returning the same response payloads, so migrating just means swapping `/workers/namespaces/` for `/storage/kv/namespaces/` in the URL path.\n\n## The story so far\n\nUntil now, you could operate on Workers KV through the legacy route under `/accounts/{account_id}/workers/namespaces/`. But the officially documented route has been the one under `/storage/kv/namespaces/`, and this announcement puts a concrete deprecation timeline on the legacy path.\n\nHere's exactly which endpoints map over:\n\n- `GET` / `POST` `.../workers/namespaces` → `.../storage/kv/namespaces` (list and create namespaces)\n- `GET` / `PUT` / `DELETE` `.../workers/namespaces/{namespace_id}` → same methods on `.../storage/kv/namespaces/{namespace_id}` (per-namespace operations)\n- `GET` `.../workers/namespaces/{namespace_id}/keys` → `.../storage/kv/namespaces/{namespace_id}/keys` (list keys)\n- `GET` `.../workers/namespaces/{namespace_id}/metadata/{key_name}` → `.../storage/kv/namespaces/{namespace_id}/metadata/{key_name}` (get metadata)\n- `GET` / `PUT` / `DELETE` `.../workers/namespaces/{namespace_id}/values/{key_name}` → same methods on `.../storage/kv/namespaces/{namespace_id}/values/{key_name}` (get, put, delete values)\n\n## What changes\n\nOnce October 15, 2026 passes, any integration or script still calling the legacy routes will simply stop working. That's most likely to hit CI/CD scripts or custom SDKs and admin tools that call the Workers KV API directly. Since the migration is a straight path substitution, once you've found every call site the actual fix should be light work.\n\n## Wrap-up\n\n- The legacy Workers KV API routes (`/workers/namespaces/*`) have been deprecated\n- They stop working on October 15, 2026\n- The new route `/storage/kv/namespaces/*` takes the same parameters and returns the same payloads — migration is just a URL path swap\n\nIf you're calling the Workers KV API directly anywhere, don't forget to make the switch!\n",
  "source_url": "https://developers.cloudflare.com/changelog/post/2026-07-15-kv-legacy-namespace-routes-deprecation/",
  "source_name": "Cloudflare Changelog",
  "og_title": "KV - Deprecate legacy Workers KV namespace API routes",
  "tags": ["cloudflare", "serverless", "devops"],
  "published_at": "2026-07-15T00:00:00+00:00",
  "emotion": "thinking",
  "importance": 2
}
